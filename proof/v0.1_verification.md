# Verification Summary Record (v0.1 Foundation & Corrective Turn)

- **Date**: 2026-07-21
- **Branch**: `codex/bobby-computer-use-v0-20260721t174022z-a9ab71173113`
- **Base SHA**: `be1141055509a88a6eedc9115f231482d0d735ab`
- **Previous Intermediate Snapshot**: `42511df783a67af36992b598d4432196ee19ba6b`
- **Status**: PASSED (100% Evidence-Based Bounded Foundation Test Suite)

---

## 1. Toolchain & Environment Verification

```text
Node.js: v22.23.1
pnpm: 10.33.0
@modelcontextprotocol/sdk: 1.29.0 (pinned exactly)
zod: 3.25.76
Swift: Apple Swift version 6.3.3 (swiftlang-6.3.3.1.3 clang-2100.1.1.101) / Target: arm64-apple-macosx26.0
OS: macOS Sonoma (14.0+)
```

---

## 2. Tested & Verified Foundation Capabilities

### A. Swift Native Host Package (`apps/computer-use-host`)
Command: `swift run ComputerUseHostTestsRunner`
Result: **PASSED** (6/6 tests passed)

```text
Build of product 'ComputerUseHostTestsRunner' complete! (0.58s)
[TEST] Running Swift Host Unit & Integration Tests...
[ALL TESTS PASSED] Swift Host verification test suite executed cleanly.
```

- **Framing & IPC Buffering**: Length-prefixed 4-byte uint32 header, max payload 16MB cap, fragmented buffer reassembly, oversized header rejection.
- **Coordinate System (`0...999` Grid)**: Continuous half-open mapping `floor(gridX / 1000.0 * widthPoints)`. 999 grid coordinate maps to 1918.0 for 1920 width, strictly remaining inside display bounds `[0, 1920)`. Handles negative screen origins (`-1920`), portrait displays (`1080x1920`), 3x Retina scaling, and rejects out-of-bounds `-1` / `1000`.
- **AX Subrole Redaction**: Strictly checks `kAXSubroleAttribute == kAXSecureTextFieldSubrole` (`subrole == "AXSecureTextField"`) to redact passwords (`[REDACTED]`).
- **HostServer Actor Concurrency**: Bounded single-use capture lease validation (`capture_id`), `topology_version`, non-empty `intent` precondition. Actions report status `dispatched` and return `post_action_observation` with fresh `capture_id`. Stale capture reuse is rejected (`STALE_CAPTURE`). Raw secret typed text is scrubbed from input history.

### B. TypeScript MCP Server Bridge (`mcp/computer-use-mcp`)
Commands: `pnpm check && pnpm test`
Result: **PASSED** (TypeScript clean, 2/2 integration tests passed)

```text
> computer-use-mcp@0.1.0 check
> tsc --noEmit

> computer-use-mcp@0.1.0 test
> pnpm build && node --test dist/test/*.test.js

TAP version 13
# Subtest: Computer Use MCP Server End-to-End Integration
    # Subtest: Exercises listTools and callTool using official SDK Client and InMemoryTransport linked pair
    ok 1 - Exercises listTools and callTool using official SDK Client and InMemoryTransport linked pair
    # Subtest: Verifies skill package sync drift between .agents/skills and skills/
    ok 2 - Verifies skill package sync drift between .agents/skills and skills/
    1..2
ok 1 - Computer Use MCP Server End-to-End Integration
# tests 2
# suites 1
# pass 2
# duration_ms 94.421333
```

- **Official SDK Client & InMemoryTransport Linked Pair**: Tests `listTools` (9 tools) and `callTool` (`computer_use_observe`, `computer_use_click`, stale error responses).
- **Dual Content Response**: Returns text metadata (without raw pixel base64) alongside MCP `ImageContent` object (`{ type: "image", data: base64, mimeType: "image/jpeg" }`).
- **Cancellation & Host Errors**: Propagates `extra.signal` into `HostClient` requests with socket abort, and converts host `success: false` into MCP tool `isError: true`.
- **Skill Package Sync**: Verified 0% drift between `.agents/skills/computer-use/SKILL.md` and `skills/computer-use/SKILL.md`.

---

## 3. Explicit Milestone Classification & Unimplemented Scope

| Component / Feature | Current Status | Notes |
|---|---|---|
| Bounded Fake Host & MCP Bridge | `IMPLEMENTED & TESTED` | Verified via Swift and Node test suites. |
| Socket Listener & Darwin Path Checks | `IMPLEMENTED & TESTED` | Fail-closed directory preparation (`0700`), socket bind/listen, `lstat` checks. |
| Real ScreenCaptureKit Capture (`SCScreenshotManager`) | `NOT IMPLEMENTED / M9 GATED` | Requires signed app bundle and user TCC Screen Recording entitlement. |
| Real Synthetic Input Injection (`CGEvent`) | `NOT IMPLEMENTED / M9 GATED` | Requires signed app bundle and user TCC Accessibility entitlement. |
| Real AX UIElement System Inspector | `NOT IMPLEMENTED / M9 GATED` | Requires active TCC Accessibility authorization. |
| Signed Host App Bundle (`ComputerUseHost.app`) | `NOT IMPLEMENTED / M9 GATED` | Production menu-bar app packaging and code signing. |
| Signed Client Token Binding (`AUTH_SECRET`) | `DESIGNED / FUTURE` | Future mitigation against same-UID unprivileged local processes. |
