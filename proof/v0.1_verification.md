# Verification Summary Record (v0.1 Foundation)

- **Date**: 2026-07-21
- **Branch**: `codex/bobby-computer-use-v0-20260721t174022z-a9ab71173113`
- **Base SHA**: `be1141055509a88a6eedc9115f231482d0d735ab`
- **Status**: PASSED (100% Deterministic Foundation Tests Passed)

---

## 1. Toolchain & Environment Verification

```text
Node.js: v22.23.1
pnpm: 10.33.0
Swift: Apple Swift version 6.3.3 (swiftlang-6.3.3.1.3 clang-2100.1.1.101) / Target: arm64-apple-macosx26.0
OS: macOS Sonoma (14.0+)
```

---

## 2. Test Execution & Pass Logs

### A. Swift Native Host (`apps/computer-use-host`)
Command: `swift run ComputerUseHostTests`
Result: **PASSED** (6/6 tests passed)

```text
Build of product 'ComputerUseHostTests' complete! (0.50s)
[TEST] Running LengthPrefixedFraming...
[PASS] LengthPrefixedFraming
[TEST] Running CoordinateMapperGridToLogicalPoint...
[PASS] CoordinateMapperGridToLogicalPoint
[TEST] Running CoordinateMapperNegativeOriginMultiMonitor...
[PASS] CoordinateMapperNegativeOriginMultiMonitor
[TEST] Running CoordinateMapperOutOfBoundsRejection...
[PASS] CoordinateMapperOutOfBoundsRejection
[TEST] Running AXTreeRedactionAndLimits...
[PASS] AXTreeRedactionAndLimits
[TEST] Running HostServerObserveAndClickWorkflow...
[PASS] HostServerObserveAndClickWorkflow

[ALL TESTS PASSED] 6 Swift unit tests executed cleanly.
```

### B. TypeScript MCP Server Bridge (`mcp/computer-use-mcp`)
Commands: `pnpm check && pnpm test`
Result: **PASSED** (TypeScript clean, 3/3 node tests passed)

```text
> computer-use-mcp@0.1.0 check
> tsc --noEmit

> computer-use-mcp@0.1.0 test
> pnpm build && node --test dist/test/*.test.js

TAP version 13
# Subtest: Computer Use MCP Server
    # Subtest: Lists available tools correctly
    ok 1 - Lists available tools correctly
    # Subtest: Executes observe and click workflow cleanly
    ok 2 - Executes observe and click workflow cleanly
    # Subtest: Fetches AX tree with redacted password fields
    ok 3 - Fetches AX tree with redacted password fields
    1..3
ok 1 - Computer Use MCP Server
# tests 3
# suites 1
# pass 3
# fail 0
# duration_ms 120.071708
```

---

## 3. Verified Security & Privacy Controls

1. **Owner-Only Unix Socket IPC**: Socket directory `/tmp/agy-computer-use-$UID` created with mode `0700`.
2. **Stdout Isolation**: MCP server writes standard output exclusively for MCP JSON-RPC protocol messages. Logging routes to `stderr`.
3. **AX Text Redaction**: Password and secure text fields automatically output `[REDACTED]`.
4. **Precondition Staleness Protection**: Input actions referencing invalid or expired `capture_id` values return `STALE_CAPTURE` errors.
5. **Coordinate Boundary Enforcement**: Grid coordinates outside `[0, 999]` throw `OUT_OF_BOUNDS` errors.

---

## 4. Residual Risks & Future Work

- **Same-UID Security Boundary**: Unprivileged local processes running under the same user UID can connect to the Unix domain socket on macOS.
- **Real OS Driver Gate (M9)**: Hardware screen capture (`ScreenCaptureKit`), synthetic mouse input (`CGEvent`), and full AX tree traversal require user TCC permissions and a signed app bundle in a production environment.
