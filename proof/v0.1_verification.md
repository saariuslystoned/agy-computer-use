# Verification Summary Record (v0.1 Foundation & P0 Corrections)

- **Date**: 2026-07-21
- **Branch**: `codex/bobby-computer-use-v0-20260721t174022z-a9ab71173113`
- **Base SHA**: `be1141055509a88a6eedc9115f231482d0d735ab`
- **Status**: PASSED (100% Deterministic Foundation Tests Passed)

---

## 1. Toolchain & Environment Verification

```text
Node.js: v22.23.1
pnpm: 10.33.0
@modelcontextprotocol/sdk: 1.29.0 (pinned exactly)
zod: 3.25.76
Swift: Apple Swift version 6.3.3 (swiftlang-6.3.3.1.3 clang-2100.1.1.101) / Target: arm64-apple-macosx26.0
OS: macOS Sonoma (14.0+)
```

---

## 2. Test Execution & Pass Logs

### A. Swift Native Host (`apps/computer-use-host`)
Command: `swift run ComputerUseHostTests`
Result: **PASSED** (6/6 tests passed)

```text
Build of product 'ComputerUseHostTests' complete! (0.52s)
[TEST] Running LengthPrefixedFramingAndBufferLimits...
[PASS] LengthPrefixedFramingAndBufferLimits
[TEST] Running GridCoordinates0To999FloorMapping...
[PASS] GridCoordinates0To999FloorMapping
[TEST] Running CoordinateMapperMultiMonitorPortraitRetina...
[PASS] CoordinateMapperMultiMonitorPortraitRetina
[TEST] Running GridCoordinateBoundaryRejection...
[PASS] GridCoordinateBoundaryRejection
[TEST] Running AXTreeSubroleRedaction...
[PASS] AXTreeSubroleRedaction
[TEST] Running HostServerPostActionObservationWorkflow...
[PASS] HostServerPostActionObservationWorkflow

[ALL TESTS PASSED] 6 Swift unit tests executed cleanly.
```

### B. TypeScript MCP Server Bridge (`mcp/computer-use-mcp`)
Commands: `pnpm check && pnpm test`
Result: **PASSED** (TypeScript clean, 3/3 node tests passed)

```text
> computer-use-mcp@0.1.0 check
> tsc --noEmit

> computer-use-mcp@0.1.0 test
> pnpm build && node --test dist/test/*.test.js

TAP version 13
# Subtest: Computer Use MCP Server Bridge
    # Subtest: Performs handshake and status check correctly
    ok 1 - Performs handshake and status check correctly
    # Subtest: Executes observe and click workflow returning post_action_observation
    ok 2 - Executes observe and click workflow returning post_action_observation
    # Subtest: Fetches AX tree with subrole redacted password fields
    ok 3 - Fetches AX tree with subrole redacted password fields
    1..3
ok 1 - Computer Use MCP Server Bridge
# tests 3
# suites 1
# pass 3
# fail 0
# duration_ms 33.325083
```

---

## 3. Verified P0 Architectural & Security Controls

1. **Grid Coordinate Mapping**: `floor(gridX / 1000.0 * widthPoints)` mapping ensures grid 999 strictly remains inside half-open display bounds `[0, width)` (e.g. 1918.0 for 1920 width). Inverses accurately, handles negative screen origins (`-1920`), portrait displays (`1080x1920`), Retina scaling (`3x`), and rejects out-of-bounds `-1` / `1000`.
2. **Post-Action Observation**: Every action (`click`, `move`, `drag`, `type`, `shortcut`, `scroll`) immediately returns `post_action_observation` with a fresh `capture_id` and image metadata, advancing state for subsequent actions.
3. **IPC Peer Credentials & Darwin Runtime Dir**: Socket path `/tmp/agy-computer-use-$UID/agy-computer-use.sock` created with `umask 077` (`chmod 0700`). Uses `getpeereid` / `SOL_LOCAL / LOCAL_PEERCRED` on macOS. Explicitly documents same-UID residual threat boundary and designs future authenticated token binding (`AUTH_SECRET` handshake). Fail-closed `lstat` owner/mode/no-symlink checks prevent symlink attacks.
4. **Official Apple AX & SC API Compliance**: Secure text field redaction strictly checks `subrole == "AXSecureTextField"` (`kAXSecureTextFieldSubrole`). Accessibility trust checked via `AXIsProcessTrustedWithOptions`. Screen capture targets macOS 14 `SCScreenshotManager.captureImage` / `SCContentFilter(display:excludingApplications:exceptingWindows:)`.
5. **Exact Dependency Pins**: `@modelcontextprotocol/sdk` pinned to exact `1.29.0`. `zod` pinned to `3.25.76`. Handler cancellation signals (`extra.signal`) propagated cleanly.
